// firebase-config.js
// -----------------------------------------------------------------------
// SUBSTITUA os valores abaixo pelas credenciais do SEU projeto Firebase.
// Console Firebase → Configurações do projeto → Seus apps → SDK setup and
// configuration → "Config".
//
// Nenhum destes valores é secreto no sentido tradicional (eles ficam
// visíveis no código-fonte do site, isso é normal para apps web Firebase),
// mas o acesso real aos dados é controlado pelas Regras do Firestore
// (veja firestore.rules) — por isso é essencial colar as regras no
// console antes de publicar o app.
// -----------------------------------------------------------------------

export const firebaseConfig = {
  apiKey: "COLE_AQUI",
  authDomain: "COLE_AQUI",
  projectId: "COLE_AQUI",
  storageBucket: "COLE_AQUI",
  messagingSenderId: "COLE_AQUI",
  appId: "COLE_AQUI"
};
